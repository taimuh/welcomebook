# データモデル: 民泊ゲストマニュアルシステム

**日付**: 2026-02-05
**ブランチ**: `001-guest-manual`

## エンティティ関係図

```
┌─────────────┐       ┌─────────────┐       ┌─────────────┐
│   Owner     │       │  Property   │       │  Category   │
│─────────────│       │─────────────│       │─────────────│
│ id          │◄──┐   │ id          │◄──┐   │ id          │
│ email       │   │   │ name        │   │   │ name        │
│ password    │   │   │ slug        │   │   │ description │
│ name        │   │   │ address     │   │   │ icon        │
│ createdAt   │   └───│ owner       │   │   │ order       │
│ updatedAt   │       │ description │   └───│ property    │
└─────────────┘       │ welcome_msg │       │ createdAt   │
                      │ emergency   │       │ updatedAt   │
                      │ createdAt   │       └──────┬──────┘
                      │ updatedAt   │              │
                      └─────────────┘              │
                                                   │
                      ┌─────────────┐              │
                      │  Content    │              │
                      │─────────────│              │
                      │ id          │              │
                      │ title       │              │
                      │ body        │◄─────────────┘
                      │ images      │
                      │ order       │
                      │ published   │
                      │ property    │
                      │ category    │
                      │ createdAt   │
                      │ updatedAt   │
                      └─────────────┘
```

## エンティティ詳細

### Owner（オーナー）

Strapi標準のUserコレクションを拡張して使用。

| フィールド | 型 | 必須 | 説明 | バリデーション |
|-----------|-----|------|------|---------------|
| id | Integer | ✓ | 主キー（自動生成） | - |
| email | Email | ✓ | メールアドレス（ログインID） | 有効なメール形式、一意 |
| password | Password | ✓ | パスワード（ハッシュ化） | 8文字以上 |
| name | String | ✓ | 表示名 | 1〜100文字 |
| createdAt | DateTime | ✓ | 作成日時（自動） | - |
| updatedAt | DateTime | ✓ | 更新日時（自動） | - |

**リレーション**:
- `properties`: Property (1対多) - 所有する物件

---

### Property（物件）

| フィールド | 型 | 必須 | 説明 | バリデーション |
|-----------|-----|------|------|---------------|
| id | Integer | ✓ | 主キー（自動生成） | - |
| name | String | ✓ | 物件名 | 1〜100文字 |
| slug | String | ✓ | URLスラッグ | `^[a-z0-9][a-z0-9-]{1,48}[a-z0-9]$`、一意 |
| address | String | - | 所在地 | 最大200文字 |
| description | Text | - | 物件説明 | 最大1000文字 |
| welcomeMessage | RichText | - | ウェルカムメッセージ | 最大2000文字 |
| emergencyContact | String | - | 緊急連絡先 | 最大200文字 |
| createdAt | DateTime | ✓ | 作成日時（自動） | - |
| updatedAt | DateTime | ✓ | 更新日時（自動） | - |

**リレーション**:
- `owner`: Owner (多対1) - 所有者
- `categories`: Category (1対多) - 物件のカテゴリ
- `contents`: Content (1対多) - 物件のコンテンツ

**ライフサイクルフック**:
- `afterCreate`: デフォルトカテゴリを4つ自動作成

---

### Category（カテゴリ）

| フィールド | 型 | 必須 | 説明 | バリデーション |
|-----------|-----|------|------|---------------|
| id | Integer | ✓ | 主キー（自動生成） | - |
| name | String | ✓ | カテゴリ名 | 1〜50文字 |
| description | String | - | カテゴリ説明 | 最大200文字 |
| icon | String | - | アイコン（絵文字） | 1〜4文字（絵文字1つ） |
| order | Integer | ✓ | 表示順 | 0以上、デフォルト0 |
| createdAt | DateTime | ✓ | 作成日時（自動） | - |
| updatedAt | DateTime | ✓ | 更新日時（自動） | - |

**リレーション**:
- `property`: Property (多対1) - 所属する物件
- `contents`: Content (1対多) - カテゴリ内のコンテンツ

**一意制約**:
- `(property, name)`: 同一物件内でカテゴリ名は一意

---

### Content（コンテンツ）

| フィールド | 型 | 必須 | 説明 | バリデーション |
|-----------|-----|------|------|---------------|
| id | Integer | ✓ | 主キー（自動生成） | - |
| title | String | ✓ | タイトル | 1〜100文字 |
| body | RichText | ✓ | 本文 | 最大50000文字 |
| images | Media | - | 画像（複数可） | 最大10枚、各5MB以下、JPEG/PNG/GIF/WebP |
| order | Integer | ✓ | 表示順 | 0以上、デフォルト0 |
| published | Boolean | ✓ | 公開状態 | デフォルトfalse（下書き） |
| createdAt | DateTime | ✓ | 作成日時（自動） | - |
| updatedAt | DateTime | ✓ | 更新日時（自動） | - |

**リレーション**:
- `property`: Property (多対1) - 所属する物件
- `category`: Category (多対1) - 所属するカテゴリ

**計算フィールド**:
- `summary`: 本文先頭100文字（API応答時に計算）

---

## デフォルトカテゴリ

物件作成時に自動生成されるカテゴリ:

| order | name | icon | description |
|-------|------|------|-------------|
| 1 | 設備の使い方 | 🏠 | 家電や設備の操作方法 |
| 2 | ハウスルール | 📋 | 滞在中のルールやマナー |
| 3 | 周辺情報 | 📍 | 近隣のお店や施設 |
| 4 | 緊急時の対応 | 🆘 | トラブル時の連絡先と対処法 |

---

## 状態遷移

### Content.published

```
[新規作成]
    │
    v
┌─────────┐     publish      ┌─────────┐
│  false  │ ───────────────► │  true   │
│ (下書き) │ ◄─────────────── │ (公開)   │
└─────────┘    unpublish     └─────────┘
```

- 新規コンテンツはデフォルトで`published: false`（下書き）
- オーナーが明示的に公開/非公開を切り替え
- ゲスト向けAPIは`published: true`のコンテンツのみ返却

---

## インデックス設計

| テーブル | インデックス | 用途 |
|---------|-------------|------|
| Property | `slug` (UNIQUE) | スラッグによる物件検索 |
| Property | `owner` | オーナーの物件一覧取得 |
| Category | `property, order` | 物件内カテゴリの順序付き取得 |
| Category | `property, name` (UNIQUE) | カテゴリ名の一意性 |
| Content | `property, published` | 公開コンテンツのフィルタリング |
| Content | `category, order` | カテゴリ内コンテンツの順序付き取得 |
| Content | `title, body` (FULLTEXT) | 検索機能（PostgreSQL） |
